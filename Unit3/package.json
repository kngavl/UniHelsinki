{
  "name": "unit3",
  "version": "1.0.0",
  "description": "Unit3 phonebook exercise",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "rm -rf build && cd ../UniHelsinki/Unit3/phonebook && npm run build --prod && cp -r build ../../../Unit3",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Turi",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.0",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^7.7.0",
    "nodemon": "^2.0.4"
  }
}
